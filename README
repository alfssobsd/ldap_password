A simple web page to change password in ldap

Setting in LdapPass.json:
	
	#LDAP server
	"ldap_host": "ldap.example.ru",

	#admin user (enable view and update password)
	"ldap_user": "cn=admin,dc=example,dc=ru",

	#password admin user
	"ldap_password" : "password",
	
	#users search base
	"ldap_search_base" : "ou=people,dc=example,dc=ru",
	
	#Password encryption algorithm (ssha,md5,crypt)
	"schema_password" : "ssha",
	
	#enable or disable samba password (1 or 0)
	enable_samba_password  = 1
	
	#password life days
	password_life = 700
